namespace WireSpire.Server.Messages {
    public enum MessageKind {
        None,
        Join,
        FinishTurn,
        GameInfo,
        EmpireFetch,
        WorldUpdate,
        MovePawn
    }
}